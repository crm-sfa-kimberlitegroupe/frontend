# Fichier d'environnement pour le développement local
# Copiez ce fichier en .env et modifiez selon vos besoins

# Backend API URL (avec /api car c'est le préfixe global du backend)
VITE_API_URL=http://localhost:3000/api
